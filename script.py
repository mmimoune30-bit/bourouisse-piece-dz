import firebase_admin; from firebase_admin import credentials, firestore; cred = credentials.Certificate('serviceAccountKey.json'); firebase_admin.initialize_app(cred); db = firestore.client(); data = {'name': 'منتج جديد', 'price': 150, 'category': 'electronics', 'in_stock': True}; update_time, doc_ref = db.collection('products').add(data); print(f'تمت إضافة الوثيقة بمُعرّف: {doc_ref.id}')
